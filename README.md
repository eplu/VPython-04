# VPython-04

📚 **VPython-04：深入 Python 列表的內建方法**

---

## 🐍 課程說明

**主題：** Python 列表 (List) 的進階內建方法 (count, index, sort, reverse, copy, extend)

**目標：** 熟練運用 List 提供的多種方法，以提升數據處理效率。

---

## 📑 目錄

1. [🔍 查詢與計數 (Search & Count)](#-查詢與計數-search--count)
   - [list.count(item)](#11-listcountitem)
   - [list.index(item, start, end)](#12-listindexitem-start-end)
2. [🔀 排序與反轉 (Sort & Reverse)](#-排序與反轉-sort--reverse)
   - [list.sort(key=None, reverse=False)](#21-listsortkeyNone-reverseFalse)
   - [list.reverse()](#22-listreverse)
3. [➕ 合併與複製 (Merge & Copy)](#-合併與複製-merge--copy)
   - [list.extend(iterable)](#31-listextenditerable)
   - [list.copy()](#32-listcopy)
4. [💡 實戰練習：內建方法應用](#-實戰練習內建方法應用)

---

## 🔍 查詢與計數 (Search & Count)

### 1.1. `list.count(item)`

計算列表中**某個特定值**出現的次數。

**範例：**

```python
grades = [85, 90, 75, 85, 100, 85]

# 計算 85 出現的次數
count_85 = grades.count(85)
print(f"85 出現的次數: {count_85}")  # 輸出: 85 出現的次數: 3

# 計算 90 出現的次數
count_90 = grades.count(90)
print(f"90 出現的次數: {count_90}")  # 輸出: 90 出現的次數: 1
```

### 1.2. `list.index(item, start, end)`

返回**第一個**匹配到特定值的**索引 (Index)**。

**注意事項：**
- ⚠️ 如果找不到該值，會產生 `ValueError` 錯誤。
- 可選參數 `start` 和 `end` 用於限定查詢的範圍。

**範例：**

```python
fruits = ["apple", "banana", "cherry", "banana", "kiwi"]

# 查找第一個 "banana" 的索引
index_banana = fruits.index("banana")
print(f"第一個 'banana' 的索引: {index_banana}")  # 輸出: 第一個 'banana' 的索引: 1

# 從索引 2 (即 "cherry") 開始查找 "banana"
index_banana_after_2 = fruits.index("banana", 2)
print(f"從索引 2 開始，'banana' 的索引: {index_banana_after_2}")  # 輸出: 從索引 2 開始，'banana' 的索引: 3
```

---

## 🔀 排序與反轉 (Sort & Reverse)

### 2.1. `list.sort(key=None, reverse=False)`

**直接修改**原列表，將其元素進行排序（預設為升序）。

**參數說明：**
- `reverse=True` 可設定為降序排序。
- `key` 可用來指定一個函數，根據該函數的返回值進行排序（例如：按字符串長度排序）。

**範例：**

```python
numbers = [40, 10, 30, 20]
words = ["apple", "Kiwi", "banana", "Cherry"]

# 預設：升序排序 (直接改變 numbers 列表)
numbers.sort()
print(f"升序排序: {numbers}")  # 輸出: 升序排序: [10, 20, 30, 40]

# 降序排序
numbers.sort(reverse=True)
print(f"降序排序: {numbers}")  # 輸出: 降序排序: [40, 30, 20, 10]

# 使用 key 參數：忽略大小寫排序
words.sort(key=str.lower)
print(f"忽略大小寫排序: {words}")  # 輸出: 忽略大小寫排序: ['apple', 'banana', 'Cherry', 'Kiwi']
```

### 2.2. `list.reverse()`

**直接修改**原列表，將元素的順序完全反轉。

**範例：**

```python
data = [1, 2, 3, 4, 5]

data.reverse()
print(f"反轉後的列表: {data}")  # 輸出: 反轉後的列表: [5, 4, 3, 2, 1]
```

---

## ➕ 合併與複製 (Merge & Copy)

### 3.1. `list.extend(iterable)`

將另一個**可迭代對象**（如另一個列表、元組等）中的所有元素**追加**到當前列表的末尾。

**注意事項：**
- ⚠️ 這與使用 `+` 運算符創建新列表不同，`extend` 是在原地修改原列表。

**範例：**

```python
list_a = [10, 20, 30]
list_b = [40, 50]

list_a.extend(list_b)  # 追加 list_b 的元素
print(f"擴展後的 A 列表: {list_a}")  # 輸出: 擴展後的 A 列表: [10, 20, 30, 40, 50]

```

### 3.2. `list.copy()`

返回列表中元素的**淺複製 (Shallow Copy)** 版本。

**重要性：**
- ⚠️ 直接使用 `new_list = old_list` 只是創建了一個指向同一列表的引用，兩者會同步變化。
- ✅ 使用 `copy()` 則創建了一個獨立的列表。

**範例：**

```python
original_list = [1, 2, 3]

# ❌ 錯誤的複製方式 (引用)
reference_list = original_list
reference_list.append(99)
print(f"原列表 (被引用影響): {original_list}")  # 輸出: 原列表 (被引用影響): [1, 2, 3, 99]

# ✅ 正確的複製方式 (獨立副本)
original_list = [1, 2, 3]  # 重置
copy_list = original_list.copy()
copy_list.append(100)

print(f"原列表 (未被副本影響): {original_list}")  # 輸出: 原列表 (未被副本影響): [1, 2, 3]
print(f"副本列表: {copy_list}")  # 輸出: 副本列表: [1, 2, 3, 100]
```

---

## 💡 實戰練習：內建方法應用

**任務：** 使用本週學習到的方法處理一個包含學生成績的列表。

**給定列表：**

```python
student_scores = [78, 92, 88, 78, 95, 88, 78, 90]
```

**練習步驟：**

1. 計算 `78` 分出現了多少次。
2. 找出 `95` 分第一次出現的索引。
3. 將列表進行降序排序。
4. 將排序後的列表反轉。
5. 建立一個新的空列表 `new_scores`，並使用 `extend` 將 `student_scores` 的內容全部加入其中。

---

## 📝 參考資源

- [Python 官方文件 - 列表方法](https://docs.python.org/zh-tw/3/tutorial/datastructures.html)
- [Python List Methods](https://www.w3schools.com/python/python_lists_methods.asp)

---

**© 2025 VPython 課程系列**
